{% extends 'email/email_base.html.twig' %}
{% block content %}
    <spacer size="20"> </spacer>
    <row>
        <columns>
            <p>{{ email.toName }} La reunion de {{ date.meeting.user.username }} a choisis une date</p>
        </columns>
    </row>
    <row>
        <columns>
            <p>La date choisis est {{ date.day|date('d/m/Y') }} à {{ date.startAt|date('H:i') }} jusqu'a {{ date.endAt|date('H:i') }}</p>
        </columns>
    </row>
    <row>
        <columns>
            <center>
                <spacer size="20"> </spacer>
                <button href="{{ url('home') }}">Connecte toi sur le site pour plus d'information</button>
                <spacer size="20"> </spacer>
            </center>
        </columns>
    </row>
{% endblock %}
