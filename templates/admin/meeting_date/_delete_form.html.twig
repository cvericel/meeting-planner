<form method="post" action="{{ path('meeting_date_delete', {'id': meeting_date.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meeting_date.id) }}">
    <button class="btn">Delete</button>
</form>
